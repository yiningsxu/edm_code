# Japan flu subtypes

Canonical script:

- `scripts/01_uic_smap_jp_flu_subtypes.R`

Supporting scripts:

- `scripts/functions_FluSubJP.R`
- `scripts/02_smap_coef_figures.R`

Legacy scripts from earlier iterations are in `legacy/`. The notebook used for figure exploration is in `notebooks/`.

Main data/output paths are still the workspace-level `data/FluSub_jp/` and `result/FluSub_JP/`.
