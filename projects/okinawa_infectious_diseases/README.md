# Okinawa infectious diseases

Scripts:

- `scripts/01_subpref_smap_interactions.R`
- `scripts/02_subpref_heatmap.R`

These scripts keep the original analysis logic but now use the shared `edm_code` bootstrap instead of machine-specific `setwd()` calls.
